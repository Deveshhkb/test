# Assets

The PNGs here are solid-color **placeholders** so the project runs out of the box.
Before store submission, replace them with real brand art:

- `icon.png` — 1024×1024, no transparency (App Store requirement)
- `adaptive-icon.png` — 1024×1024 foreground layer, keep key art inside the central 66% safe zone
- `splash.png` — centered logo on `#14110D`
