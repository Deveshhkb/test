# Production environment — loaded automatically by `next build` / `next start`.
# Only NEXT_PUBLIC_* values (safe to commit; they ship to the browser anyway).

# Live site domain
NEXT_PUBLIC_SITE_URL=https://ayodhyatour.cloudpunch.in

# Backend API (hosted on its own subdomain; Express mounts routes under /api)
NEXT_PUBLIC_API_URL=https://api.ayodhyatour.cloudpunch.in/api

# Contact
NEXT_PUBLIC_WHATSAPP_NUMBER=917305064090
NEXT_PUBLIC_CONTACT_PHONE=+917305064090
